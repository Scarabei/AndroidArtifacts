package com.google.android.gms.internal;

import android.graphics.PointF;
import java.util.ArrayList;

public final class ie implements if {
   private float x = 0.0F;
   private float y = 0.0F;
   private final boolean zzbUN = false;

   public ie(float var1, float var2, boolean var3) {
   }

   public final void zza(ArrayList var1, ArrayList var2, float var3, PointF var4) {
      if (!var2.isEmpty()) {
         var2.add((Float)var2.get(var2.size() - 1));
      } else {
         var2.add(0.0F);
      }

      PointF var5 = new PointF(this.x, this.y);
      var1.add(var5);
      var4.set(var5);
   }
}
